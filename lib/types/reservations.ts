export type fetchReservationListData = {
    month: number,
    year: number,
};

export type fetchTodayInsListData = {
    dateIn: Date,
};

export type fetchTodayOutsListData = {
    dateOut: Date,
};